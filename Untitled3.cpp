#include<stdlib.h>
#include <string.h>

int main(){

int x, y, z;
char t;

x=8;
x=7;

//t=(x % 2== 0) ? 'p': 'i'; 
printf ("%d es %s\n",x, (x % 2 == 0) ? "par": "impar");

x=10;


printf("El menor entre %d y %d es %d\n", x,y,z);

t(x % 2 ==0) ? 

return 0;
}
